# scMAEs phase 1 status

Implemented in this draft PR:

- Rank 01: `rank01_scdiva_discrete_diffusion_full`.
  - Adds a real time-conditioned masked discrete diffusion model over expression tokens.
  - Adds a runnable training/evaluation script: `run_rank01.py`.

Started but not complete:

- Rank 02: `rank02_maskfeat_gene_features_full` currently contains only a placeholder. It must not be reported as implemented.

No other ranks are implemented in this branch yet.
