# Legacy lightweight implementation

The following older files are retained only as historical work products and
must not be counted as independent full-paper implementations:

- `common/model.py`
- `common/runner.py`
- `common/variant_defs.py`
- `variants/*/run.py`
- `新模型*.csv`
- `方案尝试记录.csv`

The active objective requires independent method folders such as
`rank01_scdiva_discrete_diffusion_full` and
`rank02_maskfeat_gene_features_full`, each with its own model, loss, run script,
README, and source manifest.
