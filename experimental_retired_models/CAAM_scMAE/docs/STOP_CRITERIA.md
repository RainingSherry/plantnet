# Stop Criteria

Stop or escalate to the researcher if generator gradients are zero, mask
selection collapses, shortcut probes are high, context self-exclusion fails,
label leakage is detected, or Model C has no positive interaction term.

