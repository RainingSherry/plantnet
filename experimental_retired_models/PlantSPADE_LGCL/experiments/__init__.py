"""Experimental modules inspired by /data/luolie/缝合模块.

This package hosts small, self-contained experiments that aim to improve
(1) clustering metrics and (2) interpretability for PlantSPADE-LGCL.

Design goals:
- keep changes opt-in via CLI flags
- keep computation sparse (operate on each cell's observed support only)
"""
